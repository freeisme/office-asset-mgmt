# 版本更新说明

本文件记录每次可部署版本的变更、数据库影响和回滚提示。提交代码到 Gitea
前必须先增加对应版本说明，方便管理员在设置页选择版本时确认更新内容。

## v1.0.0

版本标识：`v1.0.0`

### 语义化版本选择

- 设置页检查更新时读取 Gitea 中已发布的 `vMAJOR.MINOR.PATCH` 版本标签。
- 发布版本按语义化版本号排序，只允许选择版本号高于当前版本且位于当前部署之后的版本。
- 下拉列表显示版本号、发布提交、提交说明和时间；没有发布标签时明确提示暂无已发布版本。
- 部署仍使用发布标签对应的提交 SHA，推送代码不会自动部署。

### 数据库变更

- 无。

### 更新注意事项

- Gitea 发布版本必须创建符合 SemVer 的注释标签，例如 `v1.0.0`。
- 发布前请同步维护本文件，并在 Gitea Releases 中补充对应说明。
- 生产更新前请先备份数据库；本版本不包含数据库迁移。

## 2026-08-05

版本标识：`manual-update-controls-20260805`

### 手动版本更新控制

- 停止 Gitea push webhook 自动触发服务器部署。
- 设置页检查更新时显示 `main` 分支最近提交，可选择目标提交版本。
- 管理员确认后才会排队部署所选提交；为保证部署脚本版本一致，只允许选择当前版本的后继提交。
- 部署脚本拒绝不属于 `origin/main` 历史的目标提交。

### 数据库变更

- 无。

### 更新注意事项

- 本次变更需要重新加载应用代码和宿主机更新控制服务。
- Gitea Webhook 可以保留，但 push 事件只记录，不再自动部署。
- 生产更新前请先备份数据库，并阅读本文件对应版本说明。
