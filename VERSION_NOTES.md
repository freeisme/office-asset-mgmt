# 版本更新说明

本文件记录每个可部署版本的功能变更、数据库影响、配置要求和回滚提示。
可部署版本必须使用未占用的 SemVer 注释标签，并在标签对应提交中包含本文件的同名
版本标题，例如 `v1.1.0`。

## v2.0.6

发布日期：2026-08-26

### 修复

- 修复库存回收页面点击已选设备后仍提示“未选定”的问题，兼容数字和字符串格式的设备标识。
- 修复多条物资部分回收失败后的重试逻辑，避免重复回收已处理记录。
- 为没有历史分配记录的旧版显示屏和非资产使用记录增加事务化兼容回收接口。

### 数据库与配置

- 不包含新的数据库迁移或环境变量变更；现有账户、资产、工单、权限和审计数据不受影响。
- 兼容回收会写入分配历史、库存变动和审计记录，库存已调整的旧记录会同步恢复库存。

### 验证与回滚

- 已执行 35 项回归测试，并通过 Python 编译、JavaScript 语法和 Git 差异检查。
- 回滚代码时仅需回退应用提交；本版本没有需要回退的数据库迁移。

## v2.0.5

发布日期：2026-08-25

### 修复

- 补齐夜间模式残留的同色文字和浅底组件，覆盖组织树、人员页、登录页、摘要卡片和提示块的可读性问题。
- 保持原有布局、交互和主题结构不变，仅修正暗色模式下的背景与前景色对比。

### 数据库与配置

- 不包含数据库结构、迁移或环境变量变更；现有账户、资产、工单、权限和审计数据不受影响。

### 验证与回滚

- 已执行前端回归测试，并确认静态资源引用已更新，浏览器可通过新版本号重新获取样式。
- 回滚代码时仅需回退应用提交；本版本没有需要回退的数据库迁移。

## v2.0.4

发布日期：2026-08-24

### 修复

- 恢复原有页面布局、组件样式和主题配色，移除会覆盖全部页面的黑白单色主题规则。
- 设置页切换分区时仅替换标签和内容区域，避免整页重新渲染造成的点击闪烁。
- 夜间模式的页面、表单设计区和流程设计区使用纯黑底色；离职设备快照与资产标签改为深色表面和浅色文字，避免浅底白字。
- 保留日间模式的原有设计风格，并确保表单字段和设计器文字使用可读前景色。

### 数据库与配置

- 不包含数据库结构、迁移或环境变量变更；现有账户、资产、工单、权限和审计数据不受影响。

### 验证与回滚

- 已执行前端回归测试，并确认 `127.0.0.1:8011` 已加载本版本的静态资源。
- 回滚代码时仅需回退应用提交；本版本没有需要回退的数据库迁移。

## v2.0.3

发布日期：2026-08-24

### 修复

- 修复 v1.2.3 历史生产库升级时，迁移登记前错误要求存在 `auth_bootstrap_guard` 而导致更新自动回滚的问题。
- 新增最先执行的幂等安全兼容迁移：创建认证启动保护表，并在缺失时补齐 `auth_session.ip_address` 与 `auth_session.user_agent`。
- 基线接管继续校验认证表与核心业务表，防止把不兼容或空白数据库错误标记为已完成历史基线。

### 部署

- 已有 v1.2.3 生产库仍需先完成备份，并临时设置 `MIGRATION_ADOPT_BASELINE=legacy-20260813`。
- 升级成功后删除该环境变量并执行迁移校验；不需要执行 `database/bootstrap/` 中的历史重建脚本。

## v2.0.2

发布日期：2026-08-21

### 修复

- 修复已有生产数据库升级到 v2 时因缺少 `schema_migration` 登记表而在迁移阶段回滚、页面保持旧版本的问题。
- 新增经过校验的一次性历史基线接管：仅允许 `legacy-20260813`，并在写入迁移登记前检查组织、人员、资产、审计、认证、库存和备份关键表。
- 保持默认保护：旧业务库未显式设置基线时仍会拒绝迁移，不会重放 `database/bootstrap/01_schema.sql` 或自动覆盖业务数据。
- 更新部署脚本为显式运行一次性 `migrate` 容器；迁移原始输出会保留在 `office-asset-gitea-webhook` 的 journal 中，方便定位失败原因。
- 增加 Docker Compose 数据库备份脚本，默认写入部署账户私有目录，并生成压缩备份和 SHA-256 校验文件，避免因应用目录由 root 持有而导致备份失败。

### 数据库与配置

- 不新增业务表或业务字段；会使用现有的 v2.0.0 增量迁移。
- 对已有且没有 `schema_migration` 的生产库，必须先备份并确认历史结构，再在服务器 `.env` 临时设置：

```dotenv
MIGRATION_ADOPT_BASELINE=legacy-20260813
```

- 迁移成功后删除该环境变量，并执行迁移校验。新安装和已登记迁移的数据库不需要设置它。

### 验证与回滚

- 已增加迁移基线、部署脚本和 Compose 备份路径回归测试；发布前执行 Python 编译、单元测试、Shell 语法和前端 JavaScript 语法检查。
- 部署前先使用 `deploy/scripts/backup_compose_database.sh` 生成并校验备份。迁移、构建或健康检查失败时，部署脚本会恢复旧应用提交，并绕过旧版的迁移依赖直接重启旧 app；数据库迁移不自动反向执行，必要时从已验证备份恢复。

## v2.0.1

发布日期：2026-08-21

### 仓库整理

- 按职责重新归类数据库初始化脚本、增量迁移、手工维护 SQL、Windows 部署脚本、迁移工具、集成回归测试和项目文档。
- 新增各目录说明与文档索引，根目录 README 明确代码、数据库、部署、测试和文档的入口。
- 保留根目录 `deploy.ps1`、`migration_runner.py`、`run_mysql_utf8.py` 和 `qa_security_regression.py` 兼容入口，现有自动化命令可继续使用。
- Docker Compose、Docker 初始化、CI、测试与部署文档已切换到新的目录路径。
- 修复 Windows 部署脚本中 `DbName` 参数与别名冲突；`-DbName` 仍保持兼容。

### 数据库与配置

- 不包含数据库结构变更，不需要执行新的迁移。
- 不新增环境变量，不修改账户、密码、资产、工单、日志或备份数据。

### 验证与回滚

- 已执行 Python 编译、单元与结构回归、前端 JavaScript 语法、PowerShell 语法、迁移文件完整性、文档链接和暂存安全扫描。
- Docker Compose 实际启动、Bash 脚本语法及需要真实数据库凭据的集成回归，应在具备对应环境的验收阶段执行。
- 回滚到 `v2.0.0` 仅恢复原有仓库目录布局；不会回滚数据库内容或任何业务数据。

## v2.0.0

发布日期：2026-08-21

### 功能与架构

- 资产写入从全量状态快照转为资源接口和原子命令：入库、领用、归还、办公终端分配和归还均在独立事务中执行，并支持幂等键和审计。
- 保留 `GET /api/state` 兼容读取；`PUT /api/state` 已退役，返回 `405 STATE_WRITE_RETIRED`。
- 引入 `office_asset/` 领域模块、SQL 仓储、权限和服务层，降低 `server.py` 的职责集中度。
- 增加同步暂存、导入追踪和数据质量审计能力。
- 增加办公终端设备流转记录及详情时间线，覆盖分配、归还和生命周期变化。
- 权限体系支持中文角色和模块名、角色类别、用户覆盖权限、查看/新增/修改/删除/审批/导出操作，以及全部/所属部门/本人/本人提交/负责工单等数据范围。
- 超级管理员由服务端角色标记判定；现有管理员获得全部权限，新建超级管理员仅可由超级管理员创建。

### IT 服务管理

- 工单模块名称统一为“工单”，并增加服务表单设计、人员和部门自动预填、表单与流程绑定。
- 增加变更管理、问题管理、知识库、SLA 计时、自动通知和多级审批。
- 服务管理页面支持常用新建工单和消息提醒入口，流程处理人员会在表单流程中体现。

### 前端与体验

- 重构总览、设置和服务管理页面的信息层级；设置采用分组卡片式布局。
- 优化工单与服务管理按钮状态、物资流转状态颜色、列表容器层级和暗色主题。
- 更新静态资源缓存版本为 `20260821-v2.0.0`，减少旧脚本和样式缓存导致的页面异常。

### 数据库与部署

- 引入 `schema_migration` 迁移登记表和 SHA-256 校验和，生产升级只应用 `migrations/` 中的新文件。
- 新增从 `20260814_001_itil_governance.sql` 到 `20260820_001_computer_movement_history.sql` 的增量迁移。
- `deploy.ps1` 增加 `-Database`/`-DbName`、目标库名校验、空库初始化与已有库显式基线采用保护。
- Docker Compose 增加 `migrate` 服务；应用仅在数据库健康且迁移成功后启动。
- Docker 空库初始化会登记 `legacy-20260813`，已有数据库缺少登记表时拒绝自动采用基线。

### 验证与回滚

- 已执行 Python 编译、前端 JavaScript 语法检查、迁移校验和接口安全回归。
- Docker Compose、Bash 脚本和浏览器关键路径应在具备对应运行环境的部署或验收阶段补充验证。
- 升级前必须备份。代码回退不会撤销迁移；数据库异常需从已验证备份恢复。

## v1.2.3

发布日期：2026-08-13

### 功能变更

- 设置页的版本更新增加“发行版”和“Beta 版”更新通道。发行版仅列出稳定的
  `vMAJOR.MINOR.PATCH` 版本；Beta 版仅列出 `vMAJOR.MINOR.PATCH-beta.N` 等预发布版本。
- 未指定更新通道时，默认使用 Beta 版；切换项目地址或更新通道后必须重新检查，避免使用
  旧检查结果执行更新。
- 优化版本更新面板布局：项目地址、版本状态、更新通道、目标版本和版本说明层级更清晰，
  宽屏双列呈现，窄屏自动收拢。
- 增加页面内容进入、面板悬停和按钮反馈动效，并遵守系统“减少动态效果”偏好。

### 数据库与配置

- 不需要数据库迁移或新增环境变量。
- 服务器更新控制服务随应用版本升级后支持 `releaseChannel=release|beta` 参数。

### 验证与回滚

- 验证发行版通道仅返回稳定注释标签，Beta 版通道仅返回预发布注释标签。
- 回滚到 `v1.2.2` 不会修改物资、流转记录、账号、数据库备份或系统设置数据；版本更新页面将
  不再显示更新通道选择。

## v1.2.2

发布日期：2026-08-13

### 修复

- 修复物资流转记录关键词、人员和来源去向筛选在连续输入或中文输入法组合输入时提前
  应用筛选、重绘列表的问题。文本输入现在仅保存草稿，点击“查询”或按 Enter 后才应用。
- 库存关键词搜索调整为同一提交式交互，避免输入过程中页面重绘导致焦点或输入状态异常。
- 操作日志的关键词和人员输入不再在失焦时自动请求接口，只会在点击“应用筛选”或按
  Enter 后查询，避免未输入完成时发出不完整查询。

### 数据库与配置

- 不需要数据库迁移，也不需要新增配置项。

### 验证与回滚

- 连续输入物品、人员、来源去向等关键词时，列表在查询前保持不变；点击“查询”或按
  Enter 后结果、数量和 Excel 导出应同步更新。
- 回滚到 `v1.2.1` 不会修改或删除任何物资流转记录、备注、库存或操作日志数据。

## v1.2.1

发布日期：2026-08-12

### 功能变更

- 物资流转记录新增多条件筛选：物品、品牌型号和备注关键词，物资类型，业务类型，
  业务分类，关联人员姓名或编号，来源或去向，以及开始和结束日期。
- 页面显示当前筛选结果数量，支持一键清除全部筛选条件。
- 新增“导出当前结果”，导出的 Excel 包含流转时间、业务类型与分类、物品、数量、
  库存影响、调出方、接收方、关联人员、触发动作和备注。

### 数据库与配置

- 不需要数据库迁移，也不需要新增配置项。

### 验证与回滚

- 升级后分别按物品、人员和日期筛选，确认结果数量和导出 Excel 一致。
- 回滚到 `v1.2.0` 不会删除任何 `inventory_movement_log` 流转记录或备注。

## v1.2.0

发布日期：2026-08-11

### 功能变更

- 将“物资流转中控”调整为“物资流转记录”，页面仅保留统一的物资流转记录表。
- 移除该页面的手工登记表单、统计卡片和筛选工具；物资入库、领用、归还、库存调整、
  人员设备回收等动作仍由对应业务页面自动写入流转记录。
- 记录表根据动作自动展示“业务类型”和“业务分类”，包括库存入库、领用发放、
  归还回收和库存管理等分类。
- 每条流转记录新增可直接编辑并保存的备注栏，用于补充工单号、交接情况或处理说明。

### 数据库与配置

- 不需要数据库迁移，也不需要新增配置项。
- 升级前建议按既有流程创建数据库备份；升级后抽查历史流转记录和备注保存功能。

### 回滚

- 回滚到 `v1.1.0` 将恢复原“物资流转中控”录入界面；已保存的流转记录和备注保留在
  `inventory_movement_log` 中，不会因前端回滚丢失。

## v1.1.0

发布日期：2026-08-07

### 功能与安全修复

- 修复登录会话过期后旧主页仍出现在登录页下方的问题，并补充静态资源缓存刷新。
- 增加请求体大小限制、密码长度上限、按 IP 和账号的登录频率限制及 HTTP 429 响应。
- 增强管理员首次初始化并发保护，记录会话创建时的客户端 IP 和 User-Agent。
- 严格校验状态快照字段和正数版本号，避免不完整数据覆盖数据库。
- 保持更新流程为管理员手动选择版本；push webhook 只记录事件，不自动部署。
- 版本列表仅接受已合并到 `main` 的 SemVer 注释标签、匹配的
  `VERSION_NOTES.md` 条目和稳定版本；设置页显示版本说明并只允许选择更高版本。
- 设置页版本更新增加项目地址输入框，可填写 GitHub 或内网 Gitea Git 地址检查可用版本；
  留空时使用服务器部署目录的默认 `origin`。
- 更新控制服务默认使用 HTTPS，并支持指定 CA 文件；部署失败时恢复之前的提交和应用镜像。
- 备份脚本改为临时文件加原子移动，增加 `--skip-lock-tables`、`--no-tablespaces` 和
  `--hex-blob`，并避免在进程参数中暴露 MySQL 密码。
- 补充库存外键、旧引用清理和办公终端术语迁移；阻止删除仍被办公终端引用的库存型号。
- GitHub Actions 仅执行源码校验，不再包含服务器 SSH 部署步骤。

### 数据库迁移

- 新增 `database/21_security_hardening.sql` 和 `database/22_update_repository_setting.sql`。
- 该迁移会补充 `auth_bootstrap_guard`、旧安装缺少的会话来源字段，清理无效库存引用，
  并为库存关联补充必要的索引和外键；同时增加可选的 `update_repository_url` 设置项。
- 生产升级前必须完成数据库备份，在测试库验证迁移，再由管理员手动执行迁移。
  更新脚本不会自动执行 SQL 迁移。

### 配置变更

- 生产环境设置 `AUTH_COOKIE_SECURE=true`，通过 HTTPS 访问应用。
- 更新控制服务配置 `DEPLOY_TLS_CERT_FILE`、`DEPLOY_TLS_KEY_FILE`、
  `UPDATE_SERVICE_CA_FILE` 和独立的控制令牌。
- 仅隔离的本地开发环境可以显式启用 HTTP 更新控制服务。

### 回滚与验证

- 更新前保留数据库备份和当前应用提交 SHA。
- 更新后检查 `/api/health`、应用和数据库容器状态、登录、版本检查及备份功能。
- 若构建、启动或健康检查失败，部署脚本会恢复前一提交并重建旧应用镜像；
  数据库迁移失败仍需使用备份或人工回滚。

## v1.0.0

发布日期：2026-08-05

### 功能

- 提供组织、人员、办公终端、IT 物资库存、操作日志和数据库备份管理。
- 设置页支持检查 Gitea 已发布版本、查看版本说明并手动选择目标版本。
- 支持亮色和暗色主题切换。

### 发布要求

- 使用 `vMAJOR.MINOR.PATCH` 格式的 SemVer 注释标签。
- 推送代码不会自动部署；生产更新必须由管理员在设置页确认。
- 生产更新前备份数据库，并按版本说明执行必要的数据库迁移。
